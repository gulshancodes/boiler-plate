
'use strict';

/* Helper Functions */

window.qs = (selector, parent = document) => parent.querySelector(selector);

window.qsa = (selector, parent = document) => parent.querySelectorAll(selector);

